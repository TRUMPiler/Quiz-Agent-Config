server:
  port: 8087

spring:
  application:
    name: QUIZ-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:5438/quiz?currentSchema=public&TimeZone=Asia/Kolkata
    username: fincons
    password: fincons123

  jpa:  
    hibernate:
      ddl-auto: update
    show-sql: true
