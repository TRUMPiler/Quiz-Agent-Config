spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/quiz
    username: fincons
    password: fincons123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true